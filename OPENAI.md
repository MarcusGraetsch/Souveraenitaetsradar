# Agent entry point

Read and follow [`AGENTS.md`](AGENTS.md) as the canonical instructions. Then read `project/PROJECT_STATE.yaml`, `project/HANDOFF.md`, `project/NEXT_ACTIONS.yaml`, and `project/DECISIONS.yaml`.
