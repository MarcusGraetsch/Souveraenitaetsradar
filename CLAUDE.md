# Agent entry point

Read and follow [`AGENTS.md`](AGENTS.md) as the canonical project instructions. Then read `project/PROJECT_STATE.yaml`, `project/HANDOFF.md`, and `project/NEXT_ACTIONS.yaml`. Do not treat this file as a separate policy source.
