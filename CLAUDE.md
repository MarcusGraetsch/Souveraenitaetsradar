# Agent entry point

Read and follow [`AGENTS.md`](AGENTS.md) as the canonical project instructions. Then read `project/PROJECT_STATE.yaml`, `project/HANDOFF.md`, `project/NEXT_ACTIONS.yaml`, and `project/DECISIONS.yaml`. Do not treat this file as a separate policy source.
