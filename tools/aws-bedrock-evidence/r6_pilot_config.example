# R6 collector example invocation (do not store credentials here)
AWS_PROFILE=your-profile
AWS_REGION=eu-central-1
AGENT_ID=
AGENT_VERSION=DRAFT
MODEL_ID=
INFERENCE_PROFILE_ID=
KMS_KEY_ID=

# ./sovradar_aws_evidence_collect.sh --profile "$AWS_PROFILE" --region "$AWS_REGION" --agent-id "$AGENT_ID" --agent-version "$AGENT_VERSION" --model-id "$MODEL_ID" --inference-profile-id "$INFERENCE_PROFILE_ID"
